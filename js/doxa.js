const article = document.getElementById('article');
const selectedArticle = document.getElementById('selectedArticle');

// Number of articles hidden in HTML code for leisure
const N = document.getElementById('counter').textContent;
const articleIndexes = Array.from({length: N}, (_, i) => i + 1);
const selectedIndex = null;




//
// articleIndexes.forEach((i) => {
//
// });
